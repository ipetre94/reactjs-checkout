// app route paths as enum in order to not be misspelled
export enum AppRoutePath {
  HomePageProducts = "/",
  CheckoutShipping = "/checkout/shipping",
  CheckoutPayment = "/checkout/payment",
  CheckoutConfirmation = "/checkout/confirmation",
  ErrorPage = "/error",
}
